# For Ruby as their is a reoccuring theme their are multiple ways to do any given task like other scripting languages
# for this looping task we will use the 'for' instead of 'while' or 'until' and the answer will be exactly the same/similar than the last
for i in 1..50 do
  print i
end