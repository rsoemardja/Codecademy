// Here we will make/create a config with phaser

const config = {
    //This is width
    width: 400,
    // This is the height
    height: 500,
    // This is the background color of our game typically hexadecimal 
    backgroundColor: 0xffff77
}

const game = new Phaser.Game(config)